
apiVersion: v1
kind: Service
metadata:
  name: {{ include "fcp-app.fullname" . }}
  labels:
    {{- include "fcp-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fcp-app.selectorLabels" . | nindent 4 }}
